# 最小路徑草案

## 第一版規則

1. **最小路徑**：`輸入 → 處理 → 輸出`，至少含 1 個主要未知能力。
2. **路徑組合**：2–5 個最小路徑可組成一個小專案；不是看數量，而是看能否形成新的完整 IPO。
3. **專案驗收**：專案必須有明確輸入、處理流程、可驗證輸出。
4. **沉澱**：專案完成後，只保留可複用的流程、模組、SOP，不把所有學習細節永久保存。
5. **升級規則**：若某個「處理」節點開始包含多個未知能力，就拆回新的最小路徑。

## 骨架

```text
最小路徑 IPO × N → 小專案 IPO × N → 能力模組 / 大專案
```

這版先不要再加層。
