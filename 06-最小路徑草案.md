# 最小路徑草案

## 第一版規則

1. **最小路徑**：`輸入 → 處理 → 輸出 → 驗證`（IPOV），原則上只包含 1 個主要未知能力；未知能力可以落在任何節點。
2. **未知能力計數**：限制的是需要真正學習的未知能力數，不限制知識點數量。其他節點可以包含多個已知或查一下即可使用的知識，均不計數。
3. **拆分規則**：如果某個節點同時出現 2 個以上彼此獨立、都需要真正學習的未知能力，代表粒度過大，應拆成新的最小路徑。
4. **驗證也可學習**：「驗證」本身可以是主要未知能力；例如學習統計檢定時，主要障礙可能就是如何驗證結果。
5. **路徑組合**：2–5 個最小路徑可組成一個小專案；不是看數量，而是看能否形成新的完整 IPOV。
6. **專案驗收**：專案必須有明確輸入、處理流程、輸出與驗證方式。
7. **沉澱**：專案完成後，只保留可複用的流程、模組、SOP，不把所有學習細節永久保存。

## 核心判定

> 一條最小路徑 ≈ 一個主要學習障礙 + 一次完整 IPOV。

## 骨架

```text
最小路徑 IPOV × N → 小專案 IPOV × N → 能力模組 / 大專案
```

這版先不要再加層。
