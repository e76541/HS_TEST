# 交接表單

| 欄位 | 內容 |
|------|------|
| 文件類型 | 交接表單（Handoff） |
| 建立日期 | 2026-08-07 |
| 狀態 | 草稿（可直接給下一輪 session / 接手者） |
| 工作目錄 | `D:\HS_PROJECT\HS_TEST\` |
| 關聯表單 | `01` 需求 · `02` 目標 · `03` 策略 · `04` 對話紀錄 |

---

## 1. 交接摘要（30 秒版）

| 項目 | 內容 |
|------|------|
| **主題** | 弄懂並準備在 Grok 裡**執行 Workflow** |
| **已完成** | 概念對齊（Plan / Workflow / Subagent）+ 說明 MD + 五份表單骨架 |
| **未完成** | **尚未實際跑過任何 Workflow**（核心需求 R-04） |
| **下一步** | 選定執行方式 → 跑通一次 → 回填需求驗收與目標表 B-06 |
| **文件根** | `D:\HS_PROJECT\HS_TEST\` |

**一句話：** 概念與文件已就緒；接手者任務 = 幫使用者**真的執行一次 Workflow**。

---

## 2. 上下文（接手必讀）

### 2.1 已對齊的心智模型

```text
PLAN  →  WORKFLOW  →  SUBAGENT(s)
```

| 概念 | 是什麼 | 不是什麼 |
|------|--------|----------|
| Plan | 藍圖：要做什麼 | 不是會自動跑的 pipeline |
| Workflow | 可執行的多 agent 編排（`.rhai` / `/workflow`） | 不是規劃表、不是單一 subagent |
| Subagent | 被派出的獨立子 session（工人） | 不是 Workflow 本身 |
| Checklist | 檢查項目 | 不是 Grok 獨立產品層 |

詳細說明：`plan-workflow-subagent.md`

### 2.2 核心需求（來自 01）

> **需求 = 執行 Workflow**（跑起來，不只理解名詞）

驗收仍待勾選：至少成功啟動並完成一次 Workflow run。

### 2.3 當前卡點（來自 02）

| ID | 卡點 | 狀態 |
|----|------|------|
| B-06 | 如何真正執行一個 Workflow？ | **未解** |
| Q-06 | 第一次用內建還是自訂 `.rhai`？ | **待答** |

---

## 3. 檔案清單（產物索引）

| 檔案 | 用途 | 接手時 |
|------|------|--------|
| `plan-workflow-subagent.md` | 概念說明 | 必讀 |
| `01-需求表單.md` | 需求與驗收 | 必讀；完成後勾選 DoD |
| `02-目標表單.md` | 卡點 / 提問 | 更新 B-06、Q-06 |
| `03-策略表單.md` | 有效互動模式 | 可選；延續教學策略 |
| `04-對話紀錄.md` | 對話彙總 | 可選；快速補脈絡 |
| `05-交接表單.md` | 本檔 | 交接入口 |

完整路徑前綴：

```text
D:\HS_PROJECT\HS_TEST\
```

---

## 4. 建議接手步驟（執行順序）

| 步驟 | 動作 | 完成標準 |
|------|------|----------|
| 1 | 讀本表 + `01` + `plan-workflow-subagent.md` | 能複述三層模型與核心需求 |
| 2 | 與使用者確認：內建 `/deep-research` **或** 自訂 `.rhai` | Q-06 有答案 |
| 3 | 執行一次 Workflow | `/workflows` 可見 run 完成或結果進對話 |
| 4 | 回填 `01` 驗收勾選、`02` 關閉 B-06 | 需求狀態可標「已滿足」或部分滿足 |
| 5 | （可選）更新 `04` 對話紀錄、`03` 策略 | 文件與實作閉環 |

### 建議預設路徑（若使用者未指定）

優先用**內建**降低門檻：

```text
/deep-research <使用者給的研究問題>
```

再帶使用者看 `/workflows` 對照 phase / agent。  
進階再寫 `.rhai` 放到專案或 `~/.grok/workflows/`。

---

## 5. 決策與約束（勿推翻除非使用者改口）

| ID | 決策 |
|----|------|
| D-01 | 工作目錄 = `D:\HS_PROJECT\HS_TEST` |
| D-02 | 核心需求 = 執行 Workflow |
| D-03 | 先建檔、後續討論（表單可迭代，不要空轉只聊天） |
| D-04 | 教學策略：先「是不是同一物」→ 對照表 → 類比 → 落盤 |

### 環境備註

- Workflow / Subagent 預設通常為開啟
- Plan mode 下不能隨便寫檔，需 Approve 後再改
- 使用者可能用 `q` 表示離開畫面，操作確認宜用明確選項

---

## 6. 交接檢查清單（交出前勾選）

- [x] 概念文件已存在
- [x] 需求 / 目標 / 策略 / 對話 四表已建
- [x] 交接表已建
- [ ] 已實際執行至少一次 Workflow
- [ ] 需求表驗收已更新
- [ ] 目標表焦點卡點已關閉
- [ ] 下一位接手人 / session 已知路徑

**交接狀態：** 文件可交；**執行任務未交完**。

---

## 7. 給下一輪 Agent 的開場提示（可複製）

```text
請讀 D:\HS_PROJECT\HS_TEST\05-交接表單.md 與 01-需求表單.md。
使用者已理解 Plan → Workflow → Subagent。
核心未完成項：實際執行一次 Workflow。
先問要用 /deep-research 還是自訂 .rhai，然後帶跑並回填表單。
```

---

## 8. 變更紀錄

| 日期 | 變更 |
|------|------|
| 2026-08-07 | 初版交接表；標示「概念完成、執行未完成」 |
